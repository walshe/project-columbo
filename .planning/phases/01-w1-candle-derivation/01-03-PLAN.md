---
phase: 01-w1-candle-derivation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - backend/java/src/test/java/walshe/projectcolumbo/rollup/CandleRollupIntegrationTest.java
autonomous: true
requirements:
  - CNDL-03
  - CNDL-05
user_setup: []

must_haves:
  truths:
    - "A real PostgreSQL database (Testcontainers) accepts W1 as a valid timeframe enum value and persists W1 candle rows"
    - "Running rollupForAllActiveAssets twice for the same source data produces no duplicate W1 rows (idempotent upsert)"
    - "Adding new D1 candles for a later complete week and re-running the rollup produces exactly one new W1 candle while previously-stored W1 candles are not re-aggregated (incremental)"
  artifacts:
    - path: "backend/java/src/test/java/walshe/projectcolumbo/rollup/CandleRollupIntegrationTest.java"
      provides: "Testcontainers integration tests for DB-enum validity, idempotency, and incremental rollup"
      min_lines: 80
      contains: "CandleRollupIntegrationTest"
  key_links:
    - from: "CandleRollupIntegrationTest"
      to: "CandleRollupService (real Spring bean)"
      via: "@Autowired against the @SpringBootTest context backed by Testcontainers Postgres"
      pattern: "@Autowired[\\s\\S]*CandleRollupService"
    - from: "CandleRollupIntegrationTest"
      to: "candle table (W1 rows)"
      via: "candleRepository.findByAssetAndTimeframe(asset, Timeframe.W1) after rollup"
      pattern: "Timeframe.W1"
---

<objective>
Verify the W1 rollup end-to-end against a real PostgreSQL database: that the V13
migration makes `W1` a usable DB enum value, that the rollup is idempotent across
repeated runs, and that it is incremental when new D1 data arrives.

Purpose: Plan 01-02's unit tests mock the repository layer, so they cannot prove the
PostgreSQL `timeframe` enum accepts `W1` (CNDL-05) nor that the
`(asset, timeframe, close_time)` unique constraint enforces idempotency (CNDL-03).
Those guarantees only hold against the real database. This plan closes that gap with
Testcontainers integration tests and is the phase's final verification gate.

Output: `CandleRollupIntegrationTest.java` — Testcontainers-backed tests for DB-enum
validity, cross-run idempotency, and incremental rollup.
</objective>

<execution_context>
@/Users/Emmett/dev/project-colombo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Emmett/dev/project-colombo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-w1-candle-derivation/01-RESEARCH.md

<interfaces>
<!-- Integration-test scaffolding extracted from the codebase. -->

Testcontainers integration-test pattern to mirror — SuperTrendRepositoryTest:
backend/java/src/test/java/walshe/projectcolumbo/persistence/repository/SuperTrendRepositoryTest.java
```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@Import(TestcontainersConfiguration.class)
class SuperTrendRepositoryTest {
    @Autowired private CandleRepository candleRepository;
    @Autowired private AssetRepository assetRepository;
    @BeforeEach void setUp() { /* deleteAll() in FK-safe order */ }
}
```

Testcontainers config (Postgres 16, applies all Flyway migrations incl. V13):
backend/java/src/test/java/walshe/projectcolumbo/TestcontainersConfiguration.java
```java
@TestConfiguration(proxyBeanMethods = false)
// @Bean @ServiceConnection PostgreSQLContainer("postgres:16-alpine")
```

Service under test — CandleRollupService (delivered by plan 01-02):
```java
@Service
@Transactional public void rollupForAllActiveAssets(Timeframe source, Timeframe target, DayOfWeek weekStartDay);
```

Repository queries for assertions — CandleRepository:
```java
List<Candle> findByAssetAndTimeframe(Asset asset, Timeframe timeframe);
List<Candle> findByAssetAndTimeframeOrderByCloseTimeAsc(Asset asset, Timeframe timeframe);
```

Entity construction — Asset has `new Asset(symbol, name, MarketProvider, active)`;
Candle is a no-arg POJO with setters (setAsset, setTimeframe, setOpenTime,
setCloseTime, setOpen/High/Low/Close, setVolume, setSource).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Testcontainers integration tests for W1 rollup</name>
  <files>backend/java/src/test/java/walshe/projectcolumbo/rollup/CandleRollupIntegrationTest.java</files>
  <read_first>
    - backend/java/src/test/java/walshe/projectcolumbo/persistence/repository/SuperTrendRepositoryTest.java (the exact @SpringBootTest + @Import(TestcontainersConfiguration.class) + @BeforeEach deleteAll pattern to mirror)
    - backend/java/src/test/java/walshe/projectcolumbo/TestcontainersConfiguration.java (Postgres 16 container — Flyway runs all migrations including V13 on startup)
    - backend/java/src/main/java/walshe/projectcolumbo/rollup/CandleRollupService.java (the service under test — from plan 01-02)
    - backend/java/src/main/java/walshe/projectcolumbo/persistence/repository/CandleRepository.java (query methods for seeding and assertions)
    - backend/java/src/main/java/walshe/projectcolumbo/persistence/entity/Candle.java (entity setters for seeding D1 fixtures)
    - .planning/phases/01-w1-candle-derivation/01-RESEARCH.md (Validation Architecture — test name map; Patterns 4-5)
  </read_first>
  <action>
    Create `CandleRollupIntegrationTest.java` in package `walshe.projectcolumbo.rollup`.
    Annotate `@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)`
    and `@Import(TestcontainersConfiguration.class)` exactly as `SuperTrendRepositoryTest`
    does. `@Autowired` the `CandleRollupService`, `CandleRepository`, and
    `AssetRepository`. In `@BeforeEach`, delete in FK-safe order
    (`candleRepository.deleteAll()` then `assetRepository.deleteAll()`).

    Helper: a private method that seeds N consecutive D1 `Candle` rows for a given
    asset starting from a given Monday `OffsetDateTime` in UTC, each with
    `openTime = startOfDay`, `closeTime = openTime + 24h - 1 nanosecond`, and simple
    deterministic OHLCV values (e.g. open=i, high=i+5, low=i, close=i+2, volume=100).
    Seed weeks that are unambiguously in the past relative to `OffsetDateTime.now()`
    (e.g. Monday 2025-01-06 UTC) so the completeness guard treats them as finalized.

    Implement these test methods:

    1. `w1_timeframeExistsInDb` (CNDL-05): seed one asset and 7 D1 candles for a past
       complete week, call
       `rollupForAllActiveAssets(Timeframe.D1, Timeframe.W1, DayOfWeek.MONDAY)`, then
       assert `candleRepository.findByAssetAndTimeframe(asset, Timeframe.W1)` returns
       exactly one row whose `getTimeframe() == Timeframe.W1`. The row persisting
       without a DB enum error proves `W1` is a valid `timeframe` enum value (the V13
       migration applied). Also assert the W1 row's OHLCV against the seeded data
       (open = Monday open, high = max daily high, low = min daily low, close = Sunday
       close, volume = sum) to confirm end-to-end correctness.

    2. `rollup_isIdempotent` (CNDL-03): seed the same 7 D1 candles, call
       `rollupForAllActiveAssets` TWICE, then assert
       `findByAssetAndTimeframe(asset, Timeframe.W1)` still returns exactly one row —
       the second run produced no duplicate (the
       `unique_asset_timeframe_close` constraint + upsert held).

    3. `rollup_isIncremental` (CNDL-03): seed 7 D1 candles for week A (past complete
       week), run the rollup, assert one W1 row. Then seed 7 more D1 candles for the
       immediately-following complete week B, run the rollup again, and assert
       `findByAssetAndTimeframeOrderByCloseTimeAsc(asset, Timeframe.W1)` returns
       exactly two rows — one new W1 candle for week B, with week A's W1 candle
       unchanged (assert week A's row id/closeTime is identical before and after the
       second run).

    Use AssertJ (`assertThat`) consistent with `SuperTrendRepositoryTest`. Because
    plan 01-02 has already delivered a working `CandleRollupService`, these tests
    should pass on first run.
  </action>
  <verify>
    <automated>cd backend/java && mvn -q test -Dtest=CandleRollupIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - File `src/test/java/walshe/projectcolumbo/rollup/CandleRollupIntegrationTest.java` exists
    - The class is annotated `@SpringBootTest` and `@Import(TestcontainersConfiguration.class)`
    - The class defines test methods `w1_timeframeExistsInDb`, `rollup_isIdempotent`, and `rollup_isIncremental`
    - `w1_timeframeExistsInDb` persists and reads back a `Timeframe.W1` candle without a PostgreSQL enum error
    - `rollup_isIdempotent` asserts exactly one W1 row after two rollup runs
    - `rollup_isIncremental` asserts exactly two W1 rows after seeding a second week and re-running, with week A's row unchanged
    - `mvn -q test -Dtest=CandleRollupIntegrationTest` exits 0
  </acceptance_criteria>
  <done>
    `CandleRollupIntegrationTest` exists and passes against Testcontainers Postgres 16:
    W1 is confirmed a valid DB enum value, the rollup is idempotent across runs, and
    it is incremental when new D1 data arrives.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the full test suite as the phase verification gate</name>
  <files>backend/java/src/test/java/walshe/projectcolumbo/rollup/CandleRollupIntegrationTest.java</files>
  <read_first>
    - backend/java/src/test/java/walshe/projectcolumbo/rollup/CandleRollupIntegrationTest.java (the file just written — confirm it compiles within the suite)
    - .planning/phases/01-w1-candle-derivation/01-RESEARCH.md (Validation Architecture — "Phase gate: full suite green before /gsd:verify-work")
  </read_first>
  <action>
    Run the complete backend test suite with `cd backend/java && mvn test` to confirm
    Phase 1 introduced no regressions. Pay specific attention to: (a) all pre-existing
    tests still pass — the new `Timeframe.W1` constant must not break any code that
    iterates `Timeframe.values()`; (b) the Flyway V13 migration applies cleanly when
    the Testcontainers Postgres starts for every integration test class, with no
    `ALTER TYPE ... cannot run inside a transaction block` error in the logs. If any
    pre-existing test fails because it asserts `Timeframe.values().length == 1` or
    similar, fix that assertion to account for the new `W1` constant and note the fix
    in the SUMMARY. Do not modify production code to make tests pass — investigate and
    fix the test assertion, or escalate if a production regression is genuine.
  </action>
  <verify>
    <automated>cd backend/java && mvn test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend/java && mvn test` exits 0 (BUILD SUCCESS)
    - Surefire reports 0 failures and 0 errors across all test classes
    - No `ALTER TYPE ... ADD VALUE cannot run inside a transaction block` appears in the build output
    - `CandleRollupServiceTest` and `CandleRollupIntegrationTest` both appear in the Surefire run and pass
  </acceptance_criteria>
  <done>
    Full `mvn test` is green: Phase 1 added W1 rollup with zero regressions to existing
    D1 behavior. The phase is ready for `/gsd:verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flyway migration → Testcontainers Postgres | V13 DDL runs against an ephemeral real database during the test run |
| Rollup service → candle table | The integration test exercises real INSERT/UPDATE paths against the unique constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | Idempotency under concurrency | mitigate | `unique_asset_timeframe_close` DB constraint is the idempotency backstop; `rollup_isIdempotent` test proves repeated runs cannot duplicate rows |
| T-01-11 | Denial of Service | Migration on every test container | mitigate | V13 is a single fast `IF NOT EXISTS` DDL; `rollup_isIncremental` confirms re-runs do not re-aggregate stored weeks (no unbounded work growth) |
| T-01-12 | Information Disclosure | Test fixtures | accept | Fixtures are synthetic OHLCV numbers — no secrets, credentials, or PII |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages — Testcontainers, JUnit 5, and Postgres driver already in pom.xml (RESEARCH.md "installs zero external dependencies") |

This plan adds test code only — no new runtime HTTP/auth surface. ASVS L1 V2-V6 do
not apply (RESEARCH.md Security Domain).
</threat_model>

<verification>
- `cd backend/java && mvn -q test -Dtest=CandleRollupIntegrationTest` exits 0
- `cd backend/java && mvn test` exits 0 with 0 failures / 0 errors (phase gate)
- Build output shows V13 applied by Flyway with no transaction-block error
</verification>

<success_criteria>
- Running rollup twice for the same week produces no duplicate W1 candle — ROADMAP
  Phase 1 Success Criterion 4 (CNDL-03, idempotent upsert)
- New D1 data produces new W1 candles incrementally without re-aggregating stored
  weeks (CNDL-03)
- W1 is confirmed a valid DB enum value against a real PostgreSQL instance — ROADMAP
  Phase 1 Success Criterion 1 (CNDL-05)
- Full backend test suite green — no regression to D1 behavior
</success_criteria>

<output>
Create `.planning/phases/01-w1-candle-derivation/01-03-SUMMARY.md` when done
</output>
