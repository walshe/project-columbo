package walshe.projectcolumbo.supertrend.api;

/** Output format selector for the summary-style read endpoints ({@code ?format=}). */
public enum SummaryFormat {
    JSON,
    MARKDOWN,
    WATCHLIST
}
