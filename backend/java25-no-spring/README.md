# SuperTrend Core — Java 25, No Spring

**Status: not yet implemented.** This module doesn't build or run anything yet.

## What this is

A from-scratch, parallel implementation of the SuperTrend indicator and its dependent APIs, built with plain Java 25 (no Spring Framework, minimal third-party libraries), applying SOLID principles. It's independent from `backend/java/` — no shared code, schema, or database — kept as a separate evaluation of how much simpler the system becomes without a framework.

See the full proposal for why, what's in/out of scope, architecture decisions, and the implementation task breakdown:

- `openspec/changes/supertrend-core-java25-rewrite/proposal.md`
- `openspec/changes/supertrend-core-java25-rewrite/design.md`
- `openspec/changes/supertrend-core-java25-rewrite/tasks.md`

## Running it

Nothing to run yet — build tool, entry point, and dev setup instructions land here once task group 1 (project foundations) is implemented.
