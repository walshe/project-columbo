---
phase: 04-multi-timeframe-scan
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanValidatorTest.java
  - backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanServiceTest.java
  - backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanIntegrationTest.java
autonomous: true
requirements: [SCAN-04, SCAN-11, SCAN-12, SCAN-13]
must_haves:
  truths:
    - "ScanValidatorTest covers: per-condition timeframe accepted, fallback to request.timeframe accepted, rejection when neither is set"
    - "ScanServiceTest contains a test for cross-timeframe AND execution"
    - "ScanIntegrationTest contains shouldExecuteMultiTimeframeScan testing W1 BULLISH AND D1 BULLISH"
    - "All pre-existing tests in ScanValidatorTest, ScanServiceTest, and ScanIntegrationTest pass"
    - "Full Maven test suite passes with BUILD SUCCESS"
  artifacts:
    - path: "backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanValidatorTest.java"
      provides: "Validator tests covering timeframe resolution cases"
      contains: "resolveTimeframe\|no.*timeframe\|condition.*timeframe"
    - path: "backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanIntegrationTest.java"
      provides: "Integration test for W1+D1 cross-timeframe AND scan"
      contains: "shouldExecuteMultiTimeframeScan"
  key_links:
    - from: "ScanIntegrationTest.shouldExecuteMultiTimeframeScan"
      to: "ScanService cross-timeframe AND logic"
      via: "seeds W1 + D1 signals for one asset, D1-only signal for another; asserts single result"
      pattern: "Timeframe\\.W1.*Timeframe\\.D1\\|shouldExecuteMultiTimeframeScan"
    - from: "ScanValidatorTest"
      to: "ScanValidator.resolveTimeframe"
      via: "three distinct test methods for the three resolution code paths"
      pattern: "shouldAccept.*PerConditionTimeframe\\|shouldAccept.*FallbackTimeframe\\|shouldReject.*NoTimeframe"
---

<objective>
Update all scan test files to compile and pass against the new DTO signatures (plans 04-01 and 04-02),
then add the multi-timeframe tests that prove the feature works end-to-end.

Three changes are required across the test suite:
1. **ScanValidatorTest** — add three new tests covering timeframe resolution (per-condition, fallback, rejection).
2. **ScanServiceTest** — update all existing `new ScanCondition(...)` and `new ScanRequest(...)` calls to match the new signatures; add one multi-timeframe unit test.
3. **ScanIntegrationTest** — update all existing condition/request constructions; add `shouldExecuteMultiTimeframeScan` end-to-end test.

After this plan, `./mvnw test` passes with BUILD SUCCESS and no regressions.

Purpose: SCAN-04 (backward compat — existing single-timeframe tests pass), SCAN-11 (validator unit tests),
SCAN-12 (multi-timeframe integration test), SCAN-13 (existing tests unmodified in behavior).
Output: Three updated test files; full Maven test suite green.
</objective>

<execution_context>
@/Users/Emmett/dev/project-colombo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Emmett/dev/project-colombo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-multi-timeframe-scan/04-02-SUMMARY.md

<interfaces>
<!-- New constructor signatures after plans 04-01 and 04-02. Read these files FIRST to verify. -->

ScanCondition (after 04-01):
  new ScanCondition(Timeframe timeframe, IndicatorType indicatorType, SignalEvent event,
                    TrendState state, Integer maxDaysSinceFlip, Integer maxDaysSinceCross)
  First arg is timeframe (nullable). All existing calls with 5 args must prepend null (or a Timeframe).

ScanRequest (after 04-01):
  new ScanRequest(Timeframe timeframe, ScanOperator operator, List<ScanCondition> conditions, Integer limit)
  Constructor signature UNCHANGED — only the @NotNull annotation was removed. Existing test calls still work.

SupertrendMatch (after 04-02):
  new SupertrendMatch(IndicatorType indicatorType, Timeframe timeframe, TrendState state,
                      SignalEvent event, int daysSinceFlip, OffsetDateTime closeTime)
  Any test that constructs SupertrendMatch must add Timeframe as second arg.

RsiMatch (after 04-02):
  new RsiMatch(IndicatorType indicatorType, Timeframe timeframe, SignalEvent event,
               double rsiValue, int daysSinceCross, OffsetDateTime closeTime)
  Any test that constructs RsiMatch must add Timeframe as second arg.

ScanService (after 04-02 — for ScanServiceTest mock updates):
  candleRepository.findLatestCloseTimeForTimeframe is now called per unique timeframe in the conditions.
  For a single-timeframe request (all conditions inherit request.timeframe()), it is still called once
  with request.timeframe().name(). Existing mocks:
    when(candleRepository.findLatestCloseTimeForTimeframe("D1")).thenReturn(Optional.of(now));
  Still work for single-D1 requests — no mock change needed for existing unit tests.
  For the new multi-timeframe unit test, you must mock BOTH:
    when(candleRepository.findLatestCloseTimeForTimeframe("D1")).thenReturn(Optional.of(now));
    when(candleRepository.findLatestCloseTimeForTimeframe("W1")).thenReturn(Optional.of(now));

ScanIntegrationTest.createSignal helper (current):
  createSignal(Asset asset, IndicatorType type, SignalEvent event, OffsetDateTime time, TrendState state)
  Sets s.setTimeframe(Timeframe.D1) — HARDCODED. Must be updated to accept Timeframe as a parameter.
  Add an overload or update the helper to take timeframe as a parameter so W1 signals can be seeded.
  Existing createSignal callers that omit timeframe should default to D1 for backward compat.

ScanIntegrationTest.createCandle helper (current):
  Sets candle.setTimeframe(Timeframe.D1) — HARDCODED.
  Add an overload or update to accept timeframe — needed to seed W1 candles for the multi-TF test.
  Existing calls default to D1.
</interfaces>

<test_patterns>
<!-- Existing tests to keep green (behavior must not change, only constructor args updated). -->

ScanValidatorTest currently constructs:
  new ScanRequest(Timeframe.D1, ScanOperator.AND, List.of(new ScanCondition(IndicatorType.SUPERTREND, ...)), null)
After update:
  new ScanCondition(null, IndicatorType.SUPERTREND, ...) — null timeframe, inherits D1 from request

ScanServiceTest currently constructs:
  new ScanCondition(IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)
After update:
  new ScanCondition(null, IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)

ScanIntegrationTest currently constructs:
  new ScanCondition(IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)
After update:
  new ScanCondition(null, IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)
  (null timeframe = inherit from ScanRequest.timeframe = Timeframe.D1 — identical behavior)
</test_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ScanValidatorTest — fix signatures, add timeframe resolution tests</name>
  <files>backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanValidatorTest.java</files>
  <read_first>
    - backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanValidatorTest.java (full current file — understand all existing test methods and their ScanCondition construction)
    - backend/java/src/main/java/walshe/projectcolumbo/api/v1/scan/dto/ScanCondition.java (after 04-01 — new 6-arg constructor)
    - backend/java/src/main/java/walshe/projectcolumbo/api/v1/scan/ScanValidator.java (after 04-02 — resolveTimeframe added)
  </read_first>
  <action>
    **Step A — Fix all existing ScanCondition constructions.**
    Every `new ScanCondition(...)` call in ScanValidatorTest uses the old 5-arg constructor. Prepend `null`
    as the first argument (the timeframe) to each call site. For example:
      Before: new ScanCondition(IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)
      After:  new ScanCondition(null, IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)
    Apply this change to EVERY ScanCondition construction in the file. Do NOT change any assertion logic.

    **Step B — Add three new @Test methods** (add after the last existing test method):

    (1) `shouldAcceptConditionWithPerConditionTimeframe`:
        Build a ScanRequest with NO top-level timeframe (null):
          new ScanRequest(null, ScanOperator.AND,
              List.of(new ScanCondition(Timeframe.W1, IndicatorType.SUPERTREND, null, TrendState.BULLISH, 5, null)),
              null)
        assertDoesNotThrow(() -> validator.validate(request));

    (2) `shouldAcceptConditionInheritingFallbackTimeframe`:
        Build a ScanRequest with top-level Timeframe.D1, condition has null timeframe:
          new ScanRequest(Timeframe.D1, ScanOperator.AND,
              List.of(new ScanCondition(null, IndicatorType.SUPERTREND, null, TrendState.BULLISH, null, null)),
              null)
        assertDoesNotThrow(() -> validator.validate(request));

    (3) `shouldRejectConditionWithNoResolvableTimeframe`:
        Build a ScanRequest with NO top-level timeframe AND condition also has null timeframe:
          new ScanRequest(null, ScanOperator.AND,
              List.of(new ScanCondition(null, IndicatorType.SUPERTREND, null, TrendState.BULLISH, null, null)),
              null)
        assertThrows(BadRequestException.class, () -> validator.validate(request));
        (BadRequestException is already imported from the existing tests — confirm the import path.)

    Add `import walshe.projectcolumbo.persistence.model.Timeframe;` if not already present.
  </action>
  <verify>
    <automated>cd backend/java && ./mvnw test -Dtest=ScanValidatorTest -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'shouldAcceptConditionWithPerConditionTimeframe' src/test/java/walshe/projectcolumbo/api/v1/scan/ScanValidatorTest.java` returns 1
    - `grep -c 'shouldAcceptConditionInheritingFallbackTimeframe' src/test/java/walshe/projectcolumbo/api/v1/scan/ScanValidatorTest.java` returns 1
    - `grep -c 'shouldRejectConditionWithNoResolvableTimeframe' src/test/java/walshe/projectcolumbo/api/v1/scan/ScanValidatorTest.java` returns 1
    - No 5-arg `new ScanCondition(IndicatorType` constructions remain (all now 6-arg with leading null or Timeframe)
    - `./mvnw test -Dtest=ScanValidatorTest` reports all tests (existing + 3 new) passing with BUILD SUCCESS
  </acceptance_criteria>
  <done>
    ScanValidatorTest compiles and passes. Three new tests cover the timeframe resolution code paths
    introduced in plan 04-02. All pre-existing validator tests pass unchanged in behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ScanServiceTest — fix signatures, add multi-timeframe unit test</name>
  <files>backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanServiceTest.java</files>
  <read_first>
    - backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanServiceTest.java (full current file — all test methods and their mock setups)
    - backend/java/src/main/java/walshe/projectcolumbo/api/v1/scan/ScanService.java (after 04-02 — understand how per-timeframe closeTime map is built; what mock calls are now triggered)
    - backend/java/src/main/java/walshe/projectcolumbo/api/v1/scan/dto/SupertrendMatch.java (after 04-02 — 6-arg constructor)
    - backend/java/src/main/java/walshe/projectcolumbo/api/v1/scan/dto/RsiMatch.java (after 04-02 — 6-arg constructor)
  </read_first>
  <action>
    **Step A — Fix all ScanCondition constructions.**
    Prepend `null` (the timeframe) as the first arg to every `new ScanCondition(...)` in the file:
      Before: new ScanCondition(IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)
      After:  new ScanCondition(null, IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)

    **Step B — Fix any SupertrendMatch or RsiMatch constructions** (if any test directly constructs them).
    Search for `new SupertrendMatch(` and `new RsiMatch(` and add the Timeframe as the second arg.
    If none exist in the test file, skip this step.

    **Step C — Add one new @Test method `execute_CrossTimeframeAND_ReturnsIntersection`.**

    Setup:
      Asset btc = new Asset(); btc.setId(1L); btc.setSymbol("BTCUSDT"); btc.setProvider(MarketProvider.BINANCE);
      Asset eth = new Asset(); eth.setId(2L); eth.setSymbol("ETHUSDT"); eth.setProvider(MarketProvider.BINANCE);
      OffsetDateTime now = OffsetDateTime.now(ZoneOffset.UTC);

      // BTC has W1 BULLISH + D1 BULLISH → should match
      SignalState btcW1 = createSignal(btc, IndicatorType.SUPERTREND, SignalEvent.NONE, now);
      btcW1.setTimeframe(Timeframe.W1); btcW1.setTrendState(TrendState.BULLISH);
      SignalState btcD1 = createSignal(btc, IndicatorType.SUPERTREND, SignalEvent.NONE, now);
      btcD1.setTimeframe(Timeframe.D1); btcD1.setTrendState(TrendState.BULLISH);

      // ETH has W1 BULLISH but NOT D1 BULLISH → should NOT match after AND intersection
      SignalState ethW1 = createSignal(eth, IndicatorType.SUPERTREND, SignalEvent.NONE, now);
      ethW1.setTimeframe(Timeframe.W1); ethW1.setTrendState(TrendState.BULLISH);

    Note: `createSignal` in ScanServiceTest creates a SignalState with setters — read the existing
    createSignal helper before writing to match the pattern. If the helper hardcodes Timeframe.D1 or a
    TrendState, set the fields after calling it.

    Mocks:
      when(candleRepository.findLatestCloseTimeForTimeframe("W1")).thenReturn(Optional.of(now));
      when(candleRepository.findLatestCloseTimeForTimeframe("D1")).thenReturn(Optional.of(now));
      when(signalStateRepository.findStateMatches(IndicatorType.SUPERTREND, TrendState.BULLISH, Timeframe.W1, null))
          .thenReturn(List.of(btcW1, ethW1));
      when(signalStateRepository.findStateMatches(IndicatorType.SUPERTREND, TrendState.BULLISH, Timeframe.D1, null))
          .thenReturn(List.of(btcD1));
      when(signalStateRepository.findLastDifferentStateTime(any(), any(), any(), any(), any()))
          .thenReturn(Optional.empty());
      when(signalStateRepository.findFirstCurrentStateTimeAfter(any(), any(), any(), any(), any(), any()))
          .thenReturn(Optional.of(now));

    Request:
      ScanRequest request = new ScanRequest(
          null, // no top-level timeframe — each condition carries its own
          ScanOperator.AND,
          List.of(
              new ScanCondition(Timeframe.W1, IndicatorType.SUPERTREND, null, TrendState.BULLISH, null, null),
              new ScanCondition(Timeframe.D1, IndicatorType.SUPERTREND, null, TrendState.BULLISH, null, null)
          ),
          null
      );

    Execute and assert:
      ScanResponse response = scanService.execute(request);
      assertThat(response.results()).hasSize(1);
      assertThat(response.results().get(0).assetSymbol()).isEqualTo("BTCUSDT");
      assertThat(response.results().get(0).matchedIndicators()).hasSize(2);
      // Both matched indicators carry their respective timeframes
      List<MatchedIndicator> matched = response.results().get(0).matchedIndicators();
      assertThat(matched).anyMatch(mi -> mi.timeframe() == Timeframe.W1);
      assertThat(matched).anyMatch(mi -> mi.timeframe() == Timeframe.D1);

    Add imports as needed: `Timeframe`, `TrendState`, `MarketProvider` (if not already present in the file).
    Read the existing import block before adding — avoid duplicates.

    If `signalStateRepository.findLastDifferentStateTime` or `findFirstCurrentStateTimeAfter` are not
    already mocked in other tests, their argument matchers may need adjustment — read the existing mock
    setups in the file to match the pattern used for TrendState.BULLISH state lookups.
  </action>
  <verify>
    <automated>cd backend/java && ./mvnw test -Dtest=ScanServiceTest -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'execute_CrossTimeframeAND_ReturnsIntersection' src/test/java/walshe/projectcolumbo/api/v1/scan/ScanServiceTest.java` returns 1
    - No 5-arg `new ScanCondition(IndicatorType` constructions remain in the file
    - The new test uses ScanRequest with null top-level timeframe and two conditions with per-condition timeframes (W1 and D1)
    - The new test asserts exactly 1 result (BTC) with 2 matched indicators carrying W1 and D1 timeframes respectively
    - `./mvnw test -Dtest=ScanServiceTest` reports all tests (existing + 1 new) passing with BUILD SUCCESS
  </acceptance_criteria>
  <done>
    ScanServiceTest compiles and passes. The new test proves the cross-timeframe AND intersection logic:
    BTC (W1 + D1 match) included; ETH (W1 only) excluded. Existing unit tests pass unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ScanIntegrationTest — fix signatures, add multi-timeframe integration test</name>
  <files>backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanIntegrationTest.java</files>
  <read_first>
    - backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanIntegrationTest.java (full current file — all tests, helpers: createAsset, createCandle, createSignal, createRsi)
    - backend/java/src/main/java/walshe/projectcolumbo/api/v1/scan/dto/ScanCondition.java (after 04-01 — 6-arg constructor; first arg is Timeframe)
    - backend/java/src/main/java/walshe/projectcolumbo/api/v1/scan/ScanService.java (after 04-02 — ScanResponse.timeframe will be null when request has no top-level timeframe)
  </read_first>
  <action>
    **Step A — Update createSignal helpers.**
    The current `createSignal(Asset, IndicatorType, SignalEvent, OffsetDateTime)` and
    `createSignal(Asset, IndicatorType, SignalEvent, OffsetDateTime, TrendState)` helpers hardcode
    `s.setTimeframe(Timeframe.D1)`. Add a new overload that accepts a `Timeframe` parameter:

      private void createSignal(Asset asset, IndicatorType type, SignalEvent event,
                                OffsetDateTime time, TrendState state, Timeframe timeframe) {
          SignalState s = new SignalState();
          s.setAsset(asset);
          s.setIndicatorType(type);
          s.setEvent(event);
          s.setCloseTime(time);
          s.setTimeframe(timeframe);
          s.setTrendState(state);
          signalStateRepository.save(s);
      }

    **Step B — Update createCandle helper.**
    The current `createCandle(Asset, OffsetDateTime)` helper hardcodes `candle.setTimeframe(Timeframe.D1)`.
    Add a new overload that accepts a `Timeframe` parameter:

      private void createCandle(Asset asset, OffsetDateTime closeTime, Timeframe timeframe) {
          Candle candle = new Candle();
          candle.setAsset(asset);
          candle.setTimeframe(timeframe);
          candle.setOpenTime(closeTime.minusDays(1));
          candle.setCloseTime(closeTime);
          candle.setOpen(java.math.BigDecimal.valueOf(100));
          candle.setHigh(java.math.BigDecimal.valueOf(110));
          candle.setLow(java.math.BigDecimal.valueOf(90));
          candle.setClose(java.math.BigDecimal.valueOf(105));
          candle.setVolume(java.math.BigDecimal.valueOf(1000));
          candle.setSource(MarketProvider.BINANCE);
          candleRepository.save(candle);
      }

    The existing 2-arg overloads are UNCHANGED — they still call setTimeframe(Timeframe.D1) internally.
    This preserves exact backward compatibility for all existing tests.

    **Step C — Fix all ScanCondition constructions in existing tests.**
    Every `new ScanCondition(IndicatorType...)` in the file uses the old 5-arg constructor. Prepend `null`
    as the first argument:
      Before: new ScanCondition(IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)
      After:  new ScanCondition(null, IndicatorType.SUPERTREND, SignalEvent.BULLISH_REVERSAL, null, null, null)
    Apply to ALL ScanCondition constructions in the file. Do NOT change any assertion logic or other code.

    **Step D — Add `shouldExecuteMultiTimeframeScan` integration test.**

    Scenario: BTC satisfies W1 SuperTrend BULLISH AND D1 SuperTrend BULLISH.
              ETH satisfies only D1 SuperTrend BULLISH. Result: only BTC returned.

    Test body:
      @Test
      void shouldExecuteMultiTimeframeScan() throws Exception {
          Asset btc = createAsset("BTCUSDT");
          Asset eth = createAsset("ETHUSDT");
          OffsetDateTime now = OffsetDateTime.now(java.time.ZoneOffset.UTC)
              .truncatedTo(java.time.temporal.ChronoUnit.DAYS);

          // BTC: W1 candle + W1 BULLISH signal + D1 candle + D1 BULLISH signal
          createCandle(btc, now, Timeframe.W1);
          createCandle(btc, now, Timeframe.D1);
          createSignal(btc, IndicatorType.SUPERTREND, SignalEvent.NONE, now, TrendState.BULLISH, Timeframe.W1);
          createSignal(btc, IndicatorType.SUPERTREND, SignalEvent.NONE, now, TrendState.BULLISH, Timeframe.D1);

          // ETH: D1 candle + D1 BULLISH signal only (no W1 data)
          createCandle(eth, now, Timeframe.D1);
          createSignal(eth, IndicatorType.SUPERTREND, SignalEvent.NONE, now, TrendState.BULLISH, Timeframe.D1);

          // Multi-timeframe scan: W1 BULLISH AND D1 BULLISH
          ScanRequest request = new ScanRequest(
              null,  // no top-level timeframe
              ScanOperator.AND,
              List.of(
                  new ScanCondition(Timeframe.W1, IndicatorType.SUPERTREND, null, TrendState.BULLISH, null, null),
                  new ScanCondition(Timeframe.D1, IndicatorType.SUPERTREND, null, TrendState.BULLISH, null, null)
              ),
              null
          );

          mockMvc.perform(post("/api/v1/scan")
                          .contentType(MediaType.APPLICATION_JSON)
                          .content(objectMapper.writeValueAsString(request)))
              .andExpect(status().isOk())
              .andExpect(jsonPath("$.timeframe").doesNotExist())         // no top-level timeframe in response
              .andExpect(jsonPath("$.results", hasSize(1)))
              .andExpect(jsonPath("$.results[0].assetSymbol").value("BTCUSDT"))
              // Each matched indicator carries its timeframe
              .andExpect(jsonPath("$.results[0].matchedIndicators", hasSize(2)))
              .andExpect(jsonPath("$.results[0].matchedIndicators[?(@.timeframe=='1W')]", hasSize(1)))
              .andExpect(jsonPath("$.results[0].matchedIndicators[?(@.timeframe=='1D')]", hasSize(1)));
      }

    Note on Timeframe JSON values: The Timeframe enum uses @JsonValue to serialize as "1D" and "1W"
    (from the `value` field in the enum, not the name). Confirm by reading Timeframe.java:
    D1 → "1D", W1 → "1W". Use these serialized values in jsonPath assertions.

    Note on `jsonPath("$.timeframe").doesNotExist()`: When ScanResponse.timeframe() returns null,
    Jackson serializes it as `"timeframe": null` by default — NOT as absent. Use
    `jsonPath("$.timeframe").value(null)` instead of `doesNotExist()` if the null field is included.
    Read the Jackson configuration in the project (ObjectMapper beans, application.properties) to confirm
    whether null fields are excluded or included, then use the correct assertion. The safest assertion is:
    `.andExpect(jsonPath("$.timeframe").doesNotExist())` for excluded nulls, or
    `.andExpect(jsonPath("$.timeframe").value(org.hamcrest.Matchers.nullValue()))` for included nulls.

    Add `import walshe.projectcolumbo.persistence.model.Timeframe;` to the file's imports if not already
    present (it may already be imported via existing tests that use Timeframe.D1 or Timeframe.W1).
  </action>
  <verify>
    <automated>cd backend/java && ./mvnw test -Dtest=ScanIntegrationTest -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'shouldExecuteMultiTimeframeScan' src/test/java/walshe/projectcolumbo/api/v1/scan/ScanIntegrationTest.java` returns 1
    - No 5-arg `new ScanCondition(IndicatorType` constructions remain (all now 6-arg starting with null or a Timeframe constant)
    - The new test seeds W1 candle + W1 signal for BTC, D1 candle + D1 signal for BTC, and D1 candle + D1 signal for ETH
    - The new test constructs ScanRequest with null top-level timeframe and two per-condition timeframes (W1 and D1)
    - The new test asserts exactly 1 result (BTCUSDT) with 2 matched indicators
    - Each matched indicator assertion references timeframe values "1W" and "1D" (the @JsonValue-serialized form)
    - `./mvnw test -Dtest=ScanIntegrationTest` passes all tests (pre-existing + new) with BUILD SUCCESS
  </acceptance_criteria>
  <done>
    ScanIntegrationTest compiles and all tests pass. shouldExecuteMultiTimeframeScan proves the
    cross-timeframe AND scan end-to-end: BTC matches (both timeframes satisfied), ETH excluded (W1 absent).
    The matched indicators each carry the correct timeframe in the JSON response.
  </done>
</task>

<task type="auto">
  <name>Task 4: Full-suite phase gate</name>
  <files>backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanIntegrationTest.java</files>
  <read_first>
    - backend/java/src/test/java/walshe/projectcolumbo/api/v1/scan/ScanIntegrationTest.java (no modification — gate only)
  </read_first>
  <action>
    Verification-only task — no code changes. Run the full Maven test suite to confirm:
    1. All three updated scan test classes pass.
    2. No regressions in other test classes (ApiIntegrationTest, W1ApiIntegrationTest, pipeline tests, etc.).
    If any test fails, diagnose and fix the root cause — do not weaken assertions or skip tests.
    Requires Docker running for Testcontainers.
  </action>
  <verify>
    <automated>cd backend/java && ./mvnw test -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw test` reports BUILD SUCCESS
    - No test failures or errors in the summary line
    - ScanValidatorTest, ScanServiceTest, ScanIntegrationTest all pass (old + new tests)
    - Pre-existing tests (ApiIntegrationTest, W1ApiIntegrationTest, MarketPipelineIntegrationTest, etc.) all pass — no regressions
  </acceptance_criteria>
  <done>
    Full Maven test suite passes with BUILD SUCCESS. The multi-timeframe scan feature is fully tested
    and no existing behavior was regressed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test infrastructure → Production code | Tests run against real PostgreSQL via Testcontainers; fixture data seeds real rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | Test fixture polluting cross-test state | mitigate | @BeforeEach setUp() already deletes all relevant tables (signalState, rsi, candle, asset repos) before each test. New createSignal/createCandle overloads use the same repositories — their data is cleaned up correctly. |
| T-04-08 | Information Disclosure | shouldExecuteMultiTimeframeScan asserting wrong timeframe keys | mitigate | Timeframe @JsonValue produces "1D" and "1W" — jsonPath filters use these serialized values, not enum names. If assertion fails due to serialization format, the test itself surfaces the issue immediately (no silent data leak). |

Test-only changes — no production security surface expanded.
</threat_model>

<verification>
- `./mvnw test -Dtest=ScanValidatorTest` passes: all pre-existing tests + 3 new timeframe-resolution tests.
- `./mvnw test -Dtest=ScanServiceTest` passes: all pre-existing tests + execute_CrossTimeframeAND test.
- `./mvnw test -Dtest=ScanIntegrationTest` passes: all pre-existing tests + shouldExecuteMultiTimeframeScan.
- `./mvnw test` BUILD SUCCESS — full suite green with no regressions.
</verification>

<success_criteria>
- ScanValidatorTest covers per-condition, fallback, and rejection timeframe paths (SCAN-11).
- ScanIntegrationTest.shouldExecuteMultiTimeframeScan proves W1+D1 AND scan end-to-end: BTC included, ETH excluded, matched indicators carry per-timeframe values (SCAN-12).
- All pre-existing scan tests pass unchanged in behavior (SCAN-04, SCAN-13).
- Full Maven test suite is green.
</success_criteria>

<output>
Create `.planning/phases/04-multi-timeframe-scan/04-03-SUMMARY.md` when done.
</output>
